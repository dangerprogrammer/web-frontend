import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cadastro | E-Eco",
  description: "Generated by create next app",
};

export default function RegisterLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
    return children;
}